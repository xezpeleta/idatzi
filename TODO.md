# TODO

## Title Bar
- [ ] Improve title bar theme handling — detect OS theme preference and apply dark/light consistently

## Console Errors
- [ ] Fix `SyntaxError: Unexpected token 'H', "HTTP/1.1 4"... is not valid JSON` — renderer calls backend before it's ready, gets HTML error instead of JSON
